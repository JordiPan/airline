{% extends 'base.html.twig' %}
{% block body %}
    <h1 class="text-center">{{ action }} {{ object }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">{{ action }}</button>
    {{ form_end(form) }}
{% endblock %}